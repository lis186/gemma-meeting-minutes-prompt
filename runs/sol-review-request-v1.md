你是資深審查者。請復盤一個「本地小模型會議記錄產生器」的執行結果。

## 系統背景
- 執行模型：gemma-4-e4b（4B，本地 LM Studio，thinking 模式，temperature 0.1）
- 輸入 = prompt 模板 + 逐字稿（無說話者標籤、無標點的 ASR 稿，含同音誤字）
- 逐字稿不可修改；只能改 prompt。
- 商務驗收標準（user 提出）：
  1. 時程需包含正確的日期（逐字稿中有「合約是 8 月 31 號要驗收」）
  2. 專有名詞需正確（ASR 誤字如 金牛→金流、postg→POS 機 需被還原），但 prompt 不能過度擬合（不能把答案硬寫進 prompt）
  3. 無法確認的部分（如可疑的公司名）需明確列出

## 檔案（你可直接讀）
- 逐字稿：execerice/transcript-software.txt
- Prompt v1：execerice/prompt-v1.txt
- gemma 輸出（3 個 seed）：execerice/runs/v1-run1.md、v1-run2.md、v1-run3.md
- run1 的 thinking 過程：execerice/runs/v1-run1.think.txt

## 你的任務（嚴格照此框架）
1. 列錯誤 + 根因。
2. 每個錯先過三關，沒全過就不必談修法：(a) 會重犯嗎？（三個 seed 的重犯率是證據）(b) 重犯一次的成本？(c) 已有規則/工具該擋它嗎——有的話，問題是「沒觸發」不是「缺規則」。
3. 每個錯標記：結構型（可用規則/工具修，這裡=可用 prompt 規則修）還是行為型（只能靠當下人為檢查）。
4. 全部排序，最後只留最高槓桿的 1 項，或明說「不值得改任何東西」。

最後加一節「商務可用性判定」：以上述三條驗收標準判定目前輸出可否作為實際商務上使用（可/不可 + 一句理由）。
